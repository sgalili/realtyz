/**
 * Session timeout DISABLED by product decision.
 * Sessions persist until the user manually signs out.
 * Kept as a no-op so existing imports continue to work.
 */
export function useSessionTimeout() {
  // intentionally empty
}
